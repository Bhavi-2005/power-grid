name: power-grid-balancer
version: "1.0.0"
description: "AI-driven power grid load balancer for renewable energy optimization"

environment:
  module: power_grid_env.env:PowerGridEnv
  dockerfile: Dockerfile

tasks:
  - name: easy
    grader: power_grid_env.graders.grader_easy:grade

  - name: medium
    grader: power_grid_env.graders.grader_medium:grade

  - name: hard
    grader: power_grid_env.graders.grader_hard:grade
